/** Default nullable materialized Prisma columns for fund row test fixtures. */
export const PRISMA_FUND_MATERIALIZED_FIELD_DEFAULTS = {
  return1y: null,
  return3y: null,
  returnYtd: null,
  returnAsOf: null,
  scoreBreakdown: null,
  peerGroupKey: null,
  peerRank: null,
} as const;
