# Local overlay for `npm run start:pro:local` (prod DB + FMP live + Expo web on localhost).
# Do not set platform secrets here — keep them in `.env.pro` (gitignored).

CORS_ORIGINS=http://localhost:8081,http://127.0.0.1:8081,http://localhost:8082,http://127.0.0.1:8082,http://localhost:19006,http://127.0.0.1:19006
SWAGGER_ENABLED=true
